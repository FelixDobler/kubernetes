apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: renovate
resources:
  - secret.yaml
  - /production/base/renovate

patches:
  - path: renovate/release-patch.yaml
    target:
      kind: HelmRelease
      namespace: renovate
