apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bentopdf
  namespace: bentopdf
spec:
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
    - example.org
  secretName: bentopdf-tls
