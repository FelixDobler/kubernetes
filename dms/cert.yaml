apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mail-tls
  namespace: dms
spec:
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
  - mail-test.doblerones.de
  secretName: mail-tls
