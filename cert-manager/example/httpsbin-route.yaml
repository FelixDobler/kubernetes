apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: httpsbin
  namespace: httpbin
spec:
  parentRefs:
    - name: example
      namespace: default
  hostnames:
    - "example.k8s.doblerones.de"
  rules:
    - backendRefs:
        - name: httpbin
          port: 8000
