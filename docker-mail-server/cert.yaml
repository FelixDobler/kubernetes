apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mail-test-cert
  namespace: mail
spec:
  secretName: mail-test-cert
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - mail-test.doblerones.de
